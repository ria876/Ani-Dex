# Jupyter Notebooks used to make the Image Recognition Models.

These are the Jupyter Notebooks use to make the models.
> Notebooks name after an animal made models to identify species of that animal.
> The ImageRec3 notebook is used to make the model for each animal grouping.
> The other notebook made the model that decides which group the image belongs to.
